---
description: "Toggle notifications on/off globally (all sessions)"
allowed-tools: "Bash(${CLAUDE_PLUGIN_ROOT}/hooks/toggle-global-notifications.sh)"
---

# Task
Toggle macOS notifications globally for all Claude Code sessions.

# Execution
Run the toggle script and report the result:
```bash
${CLAUDE_PLUGIN_ROOT}/hooks/toggle-global-notifications.sh
```

Report whether global notifications are now "enabled" or "disabled".
